{
  "name": "tkprints",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:rules": "npx --yes firebase-tools deploy --only firestore:rules",
    "deploy:storage": "npx --yes firebase-tools deploy --only storage:rules",
    "deploy:reglas": "npx --yes firebase-tools deploy --only firestore:rules,storage:rules"
  },
  "dependencies": {
    "firebase": "^12.12.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.1"
  }
}
